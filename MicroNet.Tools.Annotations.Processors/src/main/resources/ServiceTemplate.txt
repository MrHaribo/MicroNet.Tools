import micronet.activemq.AMQPeer;
import micronet.network.Request;
import micronet.network.Context;

${additional_imports}

public class ${service_class} {

	public static void main(String[] args) {
		try {
			System.out.println("Starting ${service_name}...");
	
			AMQPeer ${peer_variable} = new AMQPeer();
			Context context = new Context(${peer_variable}, "${service_uri}");
	
			${service_name} ${service_variable} = new ${service_name}();
	
			${register_listeners}
			
			${on_start}
			
			Runtime.getRuntime().addShutdownHook(new Thread() {
				public void run() {
					${on_stop}
				}
			});
		} catch (Exception e) {
			System.err.print("${service_name} crushed...\n\n");
			e.printStackTrace();
		}
	}
}
